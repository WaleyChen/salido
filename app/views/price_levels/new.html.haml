%li.list-group-item
  %a.btn.btn-default.btn-block{:href => manage_price_levels_url(:brand_id => @brand)}
    = "Back"

%li.list-group-item
  %h4= "#{@brand.name}"
  %h5= "Add Price Level"

%li.list-group-item
  = render partial: "errMsg", locals: { errMsg: @errMsg }

  = form_for(:price_level, url: price_levels_path, method: :post, :html => {:class => "form-horizontal"}) do |f|
    = f.text_field :name, :placeholder => "Name", :class => "form-control"
    %br
    = f.hidden_field "brand_id", value: @brand.id.to_s
    = f.submit "Submit", :class => "btn btn-default"