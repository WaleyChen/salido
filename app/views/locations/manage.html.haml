= render partial: "successAlert", locals: { msg: @successMsg }

%li.list-group-item
  %a.btn.btn-default.btn-block{:href => brand_url(@brand)}
    = "Back"

%li.list-group-item
  %h4= "#{@brand.name}"
  %h5= "Manage Locations"

%li.list-group-item
  %a.btn.btn-default.btn-block{:href => new_location_url(:brand_id => @brand.id)}
    = "Add"
  %a.btn.btn-default.btn-block{:href => locations_url(:brand_id => @brand.id)}
    = "Manage"